public class PipelineUtils{

    private String lesson;
    private String msgInfo;

    public String getInfo(String lesson, String msgInfo) {
        String this.lesson = lesson;
        String this.msgInfo = msgInfo;
        if ((lesson != "") && (msgInfo != "")){

        } else {

        }
    }

}
